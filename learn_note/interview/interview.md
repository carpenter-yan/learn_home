你的背景：18 年后端、川大软工硕、京东P7架构师、带3-5人团队、深耕京小智系列高并发业务、做过微服务拆分、自研组件、大促保障、系统重构、AI 营销落地、数据库多存储适配、团队管理。

面试分4大板块：技术深度八股 + 项目深挖（重中之重）、架构设计能力、团队管理 & 项目管理、软实力HR问题。
按岗位分层：高级架构/业务架构/后端架构岗，不会只考基础，所有问题全部绑定你的京东真实项目。

## 一、Java 基础 & JVM（必问，结合你的高并发场景）
JVM内存模型、垃圾回收，京东大促流量高，线上如何做GC调优？你线上用的垃圾收集器，有没有做过GC停顿优化？

并发包：ThreadPoolExecutor线程池参数，你营销延时引擎大量任务调度，线程池怎么设计？如何避免线程打满、任务堆积？

Synchronized、Lock区别，线上锁的踩坑经验；分布式锁选型（Redis/Zookeeper），你的计费中心需要账务一致性，用了哪种分布式锁？

Java SPI、类加载；Spring循环依赖三级缓存、Bean 生命周期、AOP动态代理；事务传播机制、事务失效常见场景，结合计费对账讲。
CompletableFuture多线程异步在营销平台的落地；有没有遇到过线程上下文丢失、MDC链路追踪问题，怎么解决？

## 二、Spring 全家桶、微服务、Spring Cloud Alibaba（高频）
SpringBoot自动配置原理、starter-parent 作用；JavaConfig和XML 区别。

Spring事务各种踩坑：方法内调用事务失效、final方法事务无效、多线程事务不生效，你的计费、会员同步如何保证事务可靠？

微服务拆分：你基于DDD还是业务维度拆分京小智？限界上下文怎么划分？拆分踩过什么坑（拆分过细/过粗）？ 

Nacos 注册配置、Sentinel 限流熔断、Gateway网关。大促限流规则怎么设计？接口限流、热点限流在营销触达怎么做？
分布式事务：你的计费中心、会员积分双向同步，如何保证最终一致性？TCC、本地消息表、可靠消息最终一致性选的哪种？为什么不用Seata AT？
服务雪崩防护：多级降级策略；调用超时重试怎么做，如何防止重试风暴（营销批量触达极易重复）？
### 三、存储中间件（你的简历 MySQL/TiDB/Redis/ES/Kafka/JMQ全覆盖，深挖落地）
#### MySQL/TiDB
MySQL 索引原理、最左匹配、回表、覆盖索引。营销人群圈查SQL极多，怎么做慢SQL治理？日常优化手段？

大促海量数据，分库分表方案（Sharding-JDBC）？订单、触达消息分表规则；冷热数据怎么归档？

MySQL事务隔离级别、MVCC、锁机制（行锁 / 表锁 / GAP 锁）。账务计费防超扣，如何用锁保证？

为什么部分业务选用 TiDB 而不用 MySQL？TiDB 适配的业务场景优缺点；TiDB的扩容、事务、索引和MySQL差异。

binlog 使用场景：会员通数据同步有没有用 binlog 做同步？如何解决主从延迟。

#### Redis
Redis缓存穿透、击穿、雪崩，你的营销大促如何一套方案兜底？

Redis过期策略、淘汰策略；Redis 持久化 RDB/AOF 选型；集群架构（主从、哨兵、Cluster）。

延时队列、分布式限流、分布式锁用 Redis 怎么实现？你的延时执行引擎为什么不用 Redis 延时队列，而是自研调度？优缺点对比。

多级缓存设计：本地 Caffeine + Redis，在客服、营销平台落地细节；缓存和数据库双写一致性方案。

#### MQ（Kafka、JMQ）
Kafka架构、分区、副本、ISR 机制；如何保证消息不丢、不重复消费？

营销日十亿级消息，如何保证高吞吐？消息堆积线上怎么排查、紧急扩容？

重复消费解决方案（幂等），你的计费、会员同步如何做消息幂等？

事务消息：下单触达、计费扣减如何保证本地业务和 MQ 消息原子性；Kafka 事务消息落地经验。

JMQ 和 Kafka 选型差异，京东内部 JMQ 适配场景。

#### Elasticsearch、Hive、DataPilot
ES用于人群圈选，ES分片、副本设计；海量用户检索优化；深分页、聚合查询优化方案。

离线统计使用 Hive+DataPilot，数仓分层？实时统计和离线口径不一致如何对账？

实时指标用 Redis，离线用 Hive，两套数据如何统一，适配计费对账？

### 四、核心项目深度连环追问（面试 60% 时间砸在这里，按 3 个项目拆分）
项目 1：京小智交互式营销平台（权重最高，自研三大组件是王牌）
#### 延时执行引擎
当时为什么要自研延时调度，不用RocketMQ延时、Redis ZSet、Quartz？对比各类方案优缺点。

峰值5万QPS、日十亿消息，架构分层？如何水平扩容？如何保证消息不丢失、不误触发、不重复执行？

遇到过哪些线上故障：调度延迟、节点宕机、消息堆积，怎么排查修复？

产出两项专利，简单讲下专利的技术创新点。

#### 统一计费中心
多业务线共用计费，账务强一致怎么保障？分布式计数如何避免并发超量扣费？

对账体系怎么做？日终对账流程，差错如何自动订正？

多租户计费隔离，架构怎么设计。

#### 统一权限组件
细粒度权限是RBAC还是自研？接口、菜单、数据行级权限怎么实现？多业务复用如何解耦？

大促保障：618/双十一大促全链路压测怎么做？流量预估、扩容预案、降级开关设计。

### 项目 2：京小智智能客服平台
全渠道会话高并发，海量聊天消息存储选型？为什么 ES+Redis 组合？

会话链路全链路追踪怎么做？高峰期会话卡顿如何排查？

AI 识别服务依赖第三方，超时、失败怎么降级？保证客服可用。

客服和营销平台底层能力复用怎么做？公共能力下沉思路。

### 项目 3：会员通平台重构
原有服务拆分细碎，重构收敛的整体思路？怎么评估拆分不合理，依据DDD还是调用链路？

会员、积分双向同步经常丢数据，异步补偿机制完整设计；重试策略、死信处理。

重构的风险把控：不停机灰度迁移、双写校验方案；如何做到重构期间业务零故障。

对接外部品牌多，多租户隔离怎么做。

### 五、架构设计类开放式大题（架构师必考，无标准答案，看思路）
如果让你从零设计一个营销触达平台，整体架构怎么分层？技术选型、容灾、限流、幂等、定时调度完整方案。

如何做系统容量规划？依据业务流量做机器、中间件扩容。

老旧系统重构通用方法论：如何评估重构必要性、分阶段落地、风险控制、新旧兼容。

高可用体系建设：故障演练、熔断降级、灰度发布、巡检告警完整体系，你在京东落地的规范。

怎么做技术治理：线上技术债务治理、慢 SQL 治理、冗余接口清理。

AI落地：你用 Python+LangChain 落地AI营销，AI能力如何嵌入原有Java微服务？AI接口超时、不稳定怎么兜底？

### 六、CI/CD、运维、工程化（简历提到行云、泰山、DataPilot）
你们研发流水线完整流程：代码提交、MR 评审、自动化测试、打包、灰度、全量上线。

灰度发布策略：按用户、商家灰度；如何快速回滚？

线上故障完整排查 SOP，举一次印象最深的大促故障复盘。

DataPilot 离线数据开发流程，实时数据和离线数据口径对齐方案。

### 七、团队管理、研发管理（你带 3-5 人小组，架构岗必问管理）
3~5人团队，需求拆分怎么做？如何根据成员能力分配任务？新人如何带教上手复杂营销业务？

代码评审落地机制，如何保证CR不流于形式？推动团队遵守代码规范。

团队技术规划怎么做？怎么平衡业务紧急迭代和技术优化、重构？业务忙没时间优化如何说服业务侧排期。

团队人员流动怎么应对？核心人员离职如何保证项目平稳交接。

你怎么面试后端候选人？招聘的考察标准。

### 八、职业经历相关问题
2008年前做 C/C++ ERP，后来转型 Java，转型原因？C/C++ 高可用经验给 Java 架构带来哪些帮助？

在京东近 8 年，从开发走到架构 + 小组负责人，成长路线？日常 70% 精力做架构还是业务？

同时维护营销、客服、会员三条线，多业务并行如何管理优先级？

### 九、通用架构面试经典压力题
怎么做架构取舍？可用性、一致性、性能、成本四者冲突如何权衡，举京小智案例。

接口调用链路过长，上下游依赖多，如何做解耦优化？

海量日志、埋点如何收集分析辅助线上排查？

异地多活有没有落地经验？营销多地域商家如何做多活容灾。

### 十、HR 面高频问题
2026年6月从京东离职原因（一定要客观，不谈加班、领导）。
未来3~5年技术规划，继续深耕业务架构还是偏向基础中间件？
带团队的管理理念，如何平衡管理和技术编码。

期望薪资构成，能否接受阶段性忙季加班；如何平衡工作和生活。

为什么投递本公司架构岗，对比京东业务有哪些匹配点。

### 面试备战建议
把自研延时引擎、计费中心、会员通重构梳理成标准化STAR，所有技术方案带上量化数据（QPS、数据量、优化前后指标对比）；
所有中间件不要只说用过，绑定你的业务讲选型理由、踩坑、解决方案；
准备2~3次完整线上故障复盘，包含现象、排查路径、根因、修复、长效优化；
微服务拆分、DDD、分布式事务、大促高可用是你简历最强护城河，重点打磨。